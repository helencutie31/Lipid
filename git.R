git config --global user.email "your email"
git config --global user.name "your github name"

Step 1: ADD - Choose the file u want to save (the dot: all the files you have added/fixed)
git add .
git add test_git.R
git add R/1_data_preparation.R
git add test_git.R 1_data_preparation.R

Step 2: COMMIT - package and take note abt the thing u have done
git commit -m "describe shortly what u have done"

Step 3: PUSH - push the code to the server
git push

git pull - prepare to code - make sure what u're doing is the newest

git clone example:
git clone https://github.com/jaspershen-lab/lipidflow.git